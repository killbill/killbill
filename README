In order to test the changes for the doc, one can use Jekyll to visualize the changes locally:

# Install the gem if not already present
> gem install jekyll
# Start the server
# jekyll serve --watch

Then check the results at http://127.0.0.1:4000/
